import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  body: {
    flex: 1,
    padding: 20,
    gap: 24,
  },
  context: {
    flexDirection: 'row',
    gap: 20,
  },
  contextGallery: {
    gap: 24,
  },
})
