export const regex = {
  emailRegex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/, // Verifica o formato de um email
  numberRegex: /[^\d]/g, // Verifica se existe apenas números em uma string
  codeRegex: /^\d{6}$/, // Verifica se uma string é composta por 6 números
}
